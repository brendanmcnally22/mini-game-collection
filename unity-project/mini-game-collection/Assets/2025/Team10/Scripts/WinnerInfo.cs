using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MiniGameCollection.Games2025.Team10
{
    // This is just a simple static place to remember who won in the round
    public static class WinnerInfo
    {
        // If WinnerIsRed = true then that means red won, if its false than that means blue won
        public static bool WinnerIsRed = true;
    }
}